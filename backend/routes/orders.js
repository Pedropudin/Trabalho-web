const express = require('express');
const router = express.Router();

// Implemente modelo Order e endpoints conforme necessário

router.get('/', (req, res) => {
  res.json([]);
});

module.exports = router;
